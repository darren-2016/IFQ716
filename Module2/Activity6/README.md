# IFQ716 Module 2 - Activity 6: Add some more fields to our guestbook and add a read guestbook route

The following fields are added to the guestbook:
- Age
- Gender
- Comment

Spin up the server using `npm start` and open the broswer at `localhost:3000/form`.
